# Hospital Management System - Project Document

## Technology Stack
* **Frontend:** Next.js (Vercel) - Emphasizing simple HTML/CSS where possible
* **Backend:** FastAPI (Render)
* **Database:** PostgreSQL (Neon)

## Step-by-Step Progress Tracking

### Step 1: Project Initialization
* Created the base structure: `db`, `backend`, and `frontend` folders.
* Created this `project_document.md` file to track steps and design decisions.

### Step 2: Neon Database Connection
* Created the "Hospital_Management" database in Neon.
* Retrieved the `DATABASE_URL` and applied the initial schema for `user_profiles`.

### Step 3: Phase 1 Implementation
* **Backend:** Initialized FastAPI backend in `backend/` with user profile routes.
* **Frontend:** Initialized Next.js frontend in `frontend/` with a premium landing page.
* **Database:** Applied `user_profiles` schema to Neon.
* **Codebase:** Pushed all changes to the GitHub repository.

### Step 4: Initial Deployment
* **Vercel:** Deployed the frontend (Deployment in progress).
* **Render:** Backend deployment pending (Requires manual GitHub link in Render dashboard).
